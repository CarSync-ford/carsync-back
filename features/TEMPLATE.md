# Spec: <Nome da Feature>

**Data:** <DD/MM/YYYY>  
**Autor:** <Nome>  
**Branch:** `feat/<nome-da-feature>`

---

## 1. Objetivo

<Descreva em 1-3 frases o que esta feature faz e qual problema resolve.>

---

## 2. Alterações no Banco de Dados

**Arquivo de migração:** `src/main/resources/db/migration/V<N>__<descricao>.sql`

```sql
-- DDL aqui. Exemplo:
CREATE TABLE <nome_tabela> (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    <coluna> <TIPO> NOT NULL,
    <fk_coluna> BIGINT,
    CONSTRAINT fk_<tabela>_<referencia> FOREIGN KEY (<fk_coluna>) REFERENCES <tabela_referencia>(id)
);
```

**Entidade JPA:** `entity/<NomeEntidade>.java`
- Campos: <listar campos com tipos Java>
- Relacionamentos: <descrever FKs>

> ⚠️ Se não houver alterações no banco, remova esta seção e indique: "Sem alterações no schema."

---

## 3. Contrato da API

| Método | Path | Descrição | Status |
|--------|------|-----------|--------|
| GET | `/api/v1/<recurso>` | <descrição> | 200 |
| POST | `/api/v1/<recurso>` | <descrição> | 201 |
| GET | `/api/v1/<recurso>/{id}` | <descrição> | 200 |

**Content-Types:** `application/json`, `application/xml`

---

## 4. DTOs

**Arquivo:** `dto/<Feature>Dtos.java`

```java
public final class <Feature>Dtos {
    private <Feature>Dtos() {}

    // Request
    public record <Feature>Request(
        @NotNull <Tipo> campo
    ) {}

    // Response
    public record <Feature>Response(
        Long id,
        <Tipo> campo
    ) {}
}
```

---

## 5. Regras de Negócio

1. <Regra 1: descreva a lógica que o Service deve implementar>
2. <Regra 2>
3. <Regra N>

**Exceções esperadas:**
- `ResourceNotFoundException` quando <condição>

---

## 6. Testes

**Arquivo:** `src/test/java/br/com/sprint1/challenge/<Feature>Tests.java`

- [ ] Teste de integração com `@SpringBootTest` e `TestRestTemplate`
- [ ] Cenário de sucesso para cada endpoint
- [ ] Cenário de erro (recurso não encontrado, validação inválida)
- [ ] Verificar status codes corretos (200, 201, 400, 404)

---

## 7. Critérios de Aceite

- [ ] Migração Flyway executa sem erros
- [ ] Endpoints respondem em JSON e XML
- [ ] Validação com Bean Validation funciona nos requests
- [ ] Erros retornam no formato `ApiErrorResponse`
- [ ] `mvn clean test` passa sem falhas
- [ ] Swagger documenta os novos endpoints
